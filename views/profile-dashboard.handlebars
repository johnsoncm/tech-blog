<h2>Hello</h2>

{{!-- {{#each posts as |post| }} --}}
<div class="row align-center" mb-5>
    <div class="col-md-6">
        <h2>Should be posting post title {{post.title}} {{title}} {{posts.title}}</h2>
    </div>
</div>
{{!-- {{/each}} --}}